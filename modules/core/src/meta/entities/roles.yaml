roles:
  type: table
  primaryKey: user_name
  scheme:
    columns:
    - role_name:
        type: VARCHAR(100)
        primaryKey: true
  queries:
  - All records:
      roles: Administrator
      code: |-
        SELECT * from roles